while(noSuccess)
{
        tryAgain();
        
        if(Alive)
        {
                eat();
                code();
                sleep();
                repeat();
        }
        if else(Dead)
                break();
}
